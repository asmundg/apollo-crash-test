workspace "apollo-crash-test.xcworkspace"

inhibit_all_warnings!

platform :ios, '16.0'
use_frameworks! :linkage => :static

target 'apollo-crash-test' do
  use_frameworks!
  pod 'Apollo', '1.16.0'
end
